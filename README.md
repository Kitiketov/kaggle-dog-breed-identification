перед запуском зайдите на kaggle/settings и получите api token. потом выполните команду 
mkdir -p ~/.kaggle && echo KGAT_token > ~/.kaggle/access_token && chmod 600 ~/.kaggle/access_token
тогда во второй ячейке будет корректный путь в вашей системе.
